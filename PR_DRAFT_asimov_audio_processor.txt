Branch: squad/m3-9-asimov

Summary:
- Added audio preprocessing module src/features/transcription/audioProcessor.ts implementing normalizeAudio that loads @ffmpeg/ffmpeg once and converts inputs to mono WAV 16kHz returning an ArrayBuffer. Progress is reported via a callback and a fallback estimator while loading.
- Added unit tests at src/features/transcription/audioProcessor.test.ts which mock @ffmpeg/ffmpeg to avoid WASM in CI and verify singleton behavior and output WAV header and ffmpeg args.
- Updated package.json devDependencies to include @ffmpeg/ffmpeg and @ffmpeg/core (dev).

Tests:
- Attempted to run `npx vitest run` but failed due to environment Node/Volta/npm path resolution issues: "Cannot find module ... npm-prefix.js". Tests were not executed here; locally they should pass because ffmpeg is mocked.

Notes:
- Did not commit changes; files staged. Please review and run tests locally or in CI.
- Created .squad/decisions/audio-processor.txt documenting design choices.
