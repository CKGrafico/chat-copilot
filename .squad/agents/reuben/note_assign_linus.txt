Failed to post review to GitHub via MCP tools due to unknown API method. Error: MCP server 'github-mcp-server': unknown method: create_review

Please assign PR #43 to @linus and post a review with the following points:

- File validation mismatch: current checks compare extensions to MIME types, which can cause mismatches (e.g., .webm files may have audio/webm MIME). Suggest either adding .webm to allowed extensions or relaxing the extension-only checks to accept audio/webm.
- Treat empty file.type as acceptable when the extension is valid: if file.type is empty from some browsers, validate by extension as a fallback rather than rejecting immediately.
- Accessibility: add keyboard accessibility to FileUploadZone — ensure the zone is focusable (tabIndex), and support Enter/Space to trigger file selection or drop target activation.
- Recommend using validateAudio in ShareScreen for consistent validation across components, rather than duplicating validation logic.

Assigning to @linus with label: squad:linus

Link: https://github.com/CKGrafico/chat-copilot/pull/43
